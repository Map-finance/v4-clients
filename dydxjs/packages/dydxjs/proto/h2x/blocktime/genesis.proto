syntax = "proto3";
package h2x.blocktime;

option go_package = "github.com/h2x/v4-chain/protocol/x/blocktime/types";

import "gogoproto/gogo.proto";
import "h2x/blocktime/params.proto";

// GenesisState defines the blocktime module's genesis state.
message GenesisState {
  DowntimeParams params = 1 [ (gogoproto.nullable) = false ];
}
